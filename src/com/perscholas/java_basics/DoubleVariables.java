public class DoubleVariables {
    public static void main(String[] args) {
        double x = 20.5;
        double y = 30.8;
        double sum = y/x;
        System.out.println(sum);
        int z = (int) (sum);

        System.out.println(z);

    }
}
//Write a program that declares two double variables, assigns a
// number to each, and divides the larger by the smaller number.
// Assign the result to a variable. Print out the result. Now,
// cast the result to an integer. Print out the result again.